#include <stdio.h>
int main()
{
    char ch;
    scanf("%c",&ch);
    if (ch >= 'A' && ch <= '2')
    printf("upper case");
    else if (ch >= 'a' && ch <= '2');
    printf("lower case");
    return 0;
}