#include<stdio.h>
int main ()
{
    int a,b,c;
    scanf("%d %d %d",&a,&b,&c); 
    if(a==b&&b==c){
        printf("Eqalaterial triangle");
    }   
    else if(a==b||b==c||c==a){
        printf("isosiles triangle");
    }
    else{
        printf("scalene triangle");
    }
    return 0;
}
