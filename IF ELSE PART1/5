#include<stdio.h>
int main (){
    int a;
    int b;
    scanf("%d %d",&a,&b);
    if(a>b){
        printf("%d is greater",a);
    }
    else{
        printf("%d isn greater",b);
    }
      return 0;
}
