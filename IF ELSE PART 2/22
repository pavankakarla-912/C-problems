#include <stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    if(n>=0&&n<=15){
        printf("confirmed");
    }
    else if(n>=16&&n<=20){
        printf("waiting list");
    }
    else{
        printf("invalid input");
    }
    return 0;
}
