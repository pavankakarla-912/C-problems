#include <stdio.h>
int main()
{
    int n,i;
    scanf("%d",&n);
    int marks,count=0;
    for(i=1;i<=n;i++){
        scanf("%d",&marks);
        if(marks<40){
        count++;
    }
}  
    printf("failed subjects : %d\n",count);
    printf("averge score : %d",marks/n);
    return 0;
}
