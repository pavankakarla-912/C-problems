#include <stdio.h>
int main()
{
    int n,i,units_day,sum=0;
    scanf("%d",&n);
    for (i=1;i<=n;i++);{
        scanf("%d",&units_day);
        sum=sum+units_day;
    }
    printf("total units : %d",sum);
    return 0;
}
